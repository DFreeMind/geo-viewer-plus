# Marketplace media

Store screenshots by release under `docs/media/<version>/`, for example `docs/media/0.1.0/`.
Use clean DataGrip screenshots without personal information and keep the important UI text legible. Upload the selected images to the plugin's Marketplace **Media** section when releasing that version.
